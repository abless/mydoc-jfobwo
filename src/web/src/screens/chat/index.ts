/**
 * Index file for the Chat Screen component
 * 
 * Exports the ChatScreen component which provides the user interface for interacting
 * with the LLM health advisor through natural language conversation.
 * 
 * This screen implements the F-004: LLM Health Chat feature, allowing users to send
 * messages to the AI and receive personalized health advice based on their health data.
 */

import ChatScreen from './ChatScreen';

// Export as named export for destructured imports
export { ChatScreen };

// Export as default export for simpler imports
export default ChatScreen;