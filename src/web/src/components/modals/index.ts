/**
 * index.ts
 * 
 * Central export file for all modal components used in the Health Advisor application.
 * This file aggregates all modal-related components to provide a convenient import point,
 * allowing consumers to import any modal component from a single location.
 */

import Modal from './Modal';
import BottomSheet from './BottomSheet';
import ConfirmationModal from './ConfirmationModal';

export { Modal, BottomSheet, ConfirmationModal };